services:
  pipboy-test:
    image: pipboy-test
    container_name: pipboy_test
    user: app
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./source/lib:/home/app/app/lib
      - ./test:/home/app/app/test
