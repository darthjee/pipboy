version: 2

workflows:
  version: 2
  test:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/

jobs:
  build:
    docker:
      - image: gcc
    working_directory: test
    steps:
      - checkout
      - run:
          name: DEBUG
          command: pwd
      - run:
          name: LS
          command: ls
